Color palette

background -> App default background
surface1 -> Surface 1 elements background (Navbar, Widgets, News, Settings title, Switch knob, Buttons)
surface2 -> Surface 2 elements background (Settings background)
border -> Border color
foreground -> Text color
gray -> Text comment (decs) color (Navbar UI elements strike, Switch background)
cyan -> Color
green -> Color
orange -> Color
pink -> Color
purple -> Color + Navbar UI elements active color
red -> Color
yellow -> Color
dim1 -> SeeGo text dim1, and loading dim1 color
dim2 -> SeeGo text dim2, and loading dim2 color. Its should be always blue like color, due to GoLang :F
news-title -> Keep it white color! WHITE!!!!!! #cacbcf Because in the CSS the news background gradient is forced to BLACK!
news-comment -> Keep it gray color! GRAY!!!!!! #84888f Because in the CSS the news background gradient is forced to BLACK!
